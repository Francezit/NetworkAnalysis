{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "compile release",
            "command": "./compile-release.bat"
        },
        {
            "type": "shell",
            "label": "compile debug",
            "command": "./compile-debug.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}